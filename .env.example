# API Configuration
# For production deployments, set the API URL to your PHP backend server
# Example: https://trainer.skatryk.co.ke/api.php
VITE_API_URL=https://trainer.skatryk.co.ke/api.php

# Note: Environment variables must be prefixed with VITE_ to be exposed to the client
# The app will use these URLs in order of priority:
# 1. VITE_API_URL environment variable (if set)
# 2. localStorage 'api_url' (user-set)
# 3. Native app: https://trainer.skatryk.co.ke/api.php
# 4. Web app: /api.php (relative path)
